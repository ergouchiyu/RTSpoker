# Card RTS 架构设计

## 核心概念

### 游戏模式
- **单人模式**：1个人类玩家（玩家0，底部）vs 1个AI（玩家1，顶部）
- 不做双人热座（单机测试太混乱）

### 操作权限划分

```
┌─────────────────────────────────────────────────┐
│            人类玩家（玩家0）                      │
│                                                  │
│  出牌（回合制）     派兵（回合制）    指挥单位（实时）│
│  ─────────────     ────────────    ──────────────│
│  只在自己的回合     只在自己的回合    随时可以操作    │
│  选自己的牌→出牌    花钱造兵         框选自己的兵    │
│                                    右键攻击对方牌  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│            AI玩家（玩家1）                        │
│                                                  │
│  AI出牌            AI派兵           AI指挥单位    │
│  ─────────────     ────────────    ──────────────│
│  AI回合自动出牌    攒够钱自动造     自动派兵攻击    │
└─────────────────────────────────────────────────┘
```

### 操作权限矩阵

| 操作 | 人类玩家0 | AI玩家1 |
|------|----------|---------|
| 选自己的牌 | ✅ 自己的回合 | AI自动 |
| 出牌 | ✅ 自己的回合 | AI自动 |
| 选自己的兵 | ✅ 随时 | AI自动 |
| 造兵 | ✅ 自己的回合 | AI自动 |
| 右键攻击对方牌 | ✅ 随时（只要有选中的兵）| AI自动 |
| 选对方的牌/兵 | ❌ 永远不行 | ❌ |

### 核心规则

1. **人类只能操作玩家0的东西**
   - `select_card()` → 只接受 `owner_id == 0` 的牌
   - `select_unit()` → 只接受 `owner_id == 0` 的兵
   - `spawn_unit()` → 只为玩家0造兵
   - `play_selected_cards()` → 只有玩家0的回合能出牌

2. **AI自动操作玩家1**
   - AI回合开始时自动出牌或不要
   - AI攒够钱自动造兵
   - AI的兵自动攻击人类玩家的牌

3. **回合切换时**
   - 清除所有选中状态
   - 如果切到AI回合 → AI自动行动 → 切回人类回合
   - 人类回合 → 等待人类操作

## 场景结构

```
Main (Node2D)
├── GameManager (Node)        ← 游戏逻辑
│   ├── players[0] 人类玩家   ← {id:0, is_ai:false, hand:[], units:[], supply}
│   ├── players[1] AI玩家      ← {id:1, is_ai:true, hand:[], units:[], supply}
│   ├── current_player_index  ← 当前回合玩家
│   ├── selected_cards         ← 人类选中的牌
│   └── selected_units         ← 人类选中的兵
│
├── Card (Area2D) × 20        ← 扑克牌（双方各10张）
├── Unit (Area2D) × N         ← 场上单位
│
└── UILayer (CanvasLayer)     ← UI
    ├── 信息栏（回合/补给/手牌数）
    ├── 按钮栏（出牌/不要/造兵）
    └── 消息提示
```

## 输入流程

```
鼠标点击
  ├── 点击牌 → Card._on_input_event()
  │   └── GameManager.select_card(card)
  │       └── 检查：card.owner_id == 0（人类玩家）
  │           └── 是 → 选中/取消
  │           └── 否 → 忽略
  │
  ├── 点击单位 → Unit._on_input_event()
  │   └── GameManager.select_unit(unit)
  │       └── 检查：unit.owner_id == 0（人类玩家）
  │           └── 是 → 选中/取消
  │           └── 否 → 忽略
  │
  ├── 框选 → GameManager.select_units_in_rect(rect)
  │   └── 只遍历 players[0].units（人类玩家的兵）
  │
  ├── 右键 → 检查点击位置
  │   ├── 点在敌方牌上 → command_units_attack_card(card)
  │   └── 点在空地 → command_units_move_to(pos)
  │
  └── 按钮点击
      ├── 出牌 → play_selected_cards()（只在人类回合）
      ├── 不要 → pass_turn()（只在人类回合）
      └── 造兵 → spawn_unit()（只在人类回合）

## AI 行为

```
AI回合开始
  ├── AI出牌逻辑
  │   ├── 找最小的合法牌型出
  │   └── 出不了 → 不要
  │
  └── AI回合结束 → 切回人类回合

AI经济逻辑（每帧）
  └── 攒够钱 → 自动造兵

AI战斗逻辑（每帧）
  └── 有兵 → 自动派去攻击人类玩家的牌
```
